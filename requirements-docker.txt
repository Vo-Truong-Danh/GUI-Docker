# Minimal requirements for Spark Worker (Python 3.7 compatible)
# Keep it minimal - only essential packages for Spark data processing

# Data processing - core packages only
PyYAML==5.3.1
pandas==1.3.5
numpy==1.21.6

# Utilities
requests==2.28.2
python-dotenv==0.21.1

# NOTE: Removed heavy packages (scipy, scikit-learn, matplotlib, opencv, lxml)
# because they require compilation from source and often fail in Alpine.
# Install them in your Spark job if truly needed, or use a Debian-based image.

